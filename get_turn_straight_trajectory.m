function [trajectory] = get_turn_straight_trajectory(initial_state,turn_char,straight_char,final_position)
    %state.pos = [x;y];
    %state.vel = [x;y];

    %turn_char.spd = (scalar magnitude)
    %turn_char.acc = (scalar magnitude)
    %turn_char.R = (spd^2/a)
    %turn_char.w = (spd/a)
    
    %straight_char.spd = (scalar magnitude)

    %final_position = [x;y];

end